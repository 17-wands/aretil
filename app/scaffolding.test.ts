import { describe, expect, it } from "vitest";

// Placeholder test confirming the TypeScript toolchain runs.
// Replaced by real tests as the app layer is built (issues #12, #13).
describe("scaffolding", () => {
  it("confirms the TypeScript toolchain runs", () => {
    expect(true).toBe(true);
  });
});

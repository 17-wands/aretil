{
  "name": "@aretil/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "aretil — TypeScript web app (Vite frontend plus a thin Node API)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "jobs:validate": "echo 'jobs:validate: no data jobs defined yet'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@duckdb/node-api": "1.5.3-r.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^6.0.3",
    "vite": "^5.1.0",
    "vitest": "^4.1.6"
  }
}

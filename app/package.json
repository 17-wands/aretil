{
  "name": "@aretil/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "aretil — TypeScript web app (Vite frontend plus a thin Node API)",
  "scripts": {
    "dev": "echo 'dev: Vite dev server is added in issue #13'",
    "build": "tsc",
    "test": "vitest run",
    "test:e2e": "echo 'test:e2e: Playwright e2e tests are added in issue #13'",
    "jobs:validate": "echo 'jobs:validate: no data jobs defined yet'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@duckdb/node-api": "1.5.3-r.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
